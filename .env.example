# Copy this file to .env and fill in your Anthropic API key.
#
# This key is read by the browser and sent directly to the Claude API, so it is
# visible to anyone who can open the page. Run this locally only, and use a key
# you are able to rotate.
VITE_ANTHROPIC_API_KEY=sk-ant-...
